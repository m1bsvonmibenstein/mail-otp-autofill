# Rust build output
native-app/target/

# Compiled installer output (attach to a GitHub Release instead)
installer/Output/

# Generated native-messaging manifests (contain absolute local paths)
native-app/host-manifest/

# Generated autostart launcher (absolute local path)
native-app/launch-daemon.vbs

# Signing / private keys - never commit
*.pem
otp-autofill-signing-key.pem

# Spike/debug logs
otp_relay_spike.log

# Key helper artifacts (public key already lives in manifest.json)
extension/.ext-key.txt
extension/.ext-id.txt

# OS cruft
Thumbs.db
.DS_Store

# Distribution build output
dist/
